import React from 'react';
import T from 'prop-types';

const Message = (props) => {
    return (
        <div></div>
    );
};

Message.propTypes = {};
Message.defaultProps = {};

export default Message;
